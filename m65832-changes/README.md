# M65832 change artifacts

This folder centralizes all M65832-related change artifacts to keep them in one place.

Contents:
- `patches/` – patch files that can be applied after a clean rebase.

Notes:
- For upstream-sourced trees (like `picolibc-m65832`), keep a patch here even if
  we also commit the change directly.
